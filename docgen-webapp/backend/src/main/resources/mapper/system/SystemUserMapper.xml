<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tongtu.docgen.system.mapper.SystemUserMapper">

    <resultMap id="SysUserMap" type="com.tongtu.docgen.system.model.entity.SysUserEntity">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="displayName" column="display_name"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByUsername" resultMap="SysUserMap">
        SELECT id, username, password_hash, display_name, status, created_at, updated_at
        FROM sys_user
        WHERE username = #{username}
        LIMIT 1
    </select>

    <select id="findById" resultMap="SysUserMap">
        SELECT id, username, password_hash, display_name, status, created_at, updated_at
        FROM sys_user
        WHERE id = #{id}
        LIMIT 1
    </select>

    <select id="listUsers" resultMap="SysUserMap">
        SELECT id, username, password_hash, display_name, status, created_at, updated_at
        FROM sys_user
        ORDER BY id DESC
    </select>

    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password_hash, display_name, status, created_at, updated_at)
        VALUES (#{username}, #{passwordHash}, #{displayName}, #{status}, NOW(), NOW())
    </insert>

    <update id="updateUser">
        UPDATE sys_user
        SET display_name = #{displayName},
            status = #{status},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE sys_user
        SET password_hash = #{passwordHash},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <select id="findRoleCodesByUserId" resultType="string">
        SELECT r.role_code
        FROM sys_role r
                 JOIN sys_user_role ur ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
        ORDER BY r.role_code
    </select>

    <select id="findRoleIdsByUserId" resultType="long">
        SELECT ur.role_id
        FROM sys_user_role ur
        WHERE ur.user_id = #{userId}
        ORDER BY ur.role_id
    </select>

    <select id="findPermissionCodesByUserId" resultType="string">
        SELECT DISTINCT p.perm_code
        FROM sys_permission p
                 JOIN sys_role_permission rp ON rp.permission_id = p.id
                 JOIN sys_user_role ur ON ur.role_id = rp.role_id
        WHERE ur.user_id = #{userId}
        ORDER BY p.perm_code
    </select>

    <delete id="deleteUserRoles">
        DELETE
        FROM sys_user_role
        WHERE user_id = #{userId}
    </delete>

    <insert id="insertUserRole">
        INSERT INTO sys_user_role (user_id, role_id)
        VALUES (#{userId}, #{roleId})
        ON CONFLICT (user_id, role_id) DO NOTHING
    </insert>
</mapper>


