agent:
  llm:
    openai:
      # OpenAI-compatible endpoint
      baseUrl: "https://api.openai.com/v1"
      model: "gpt-4o-mini"
      # Local-only config. Do NOT commit real keys.
      apiKey: ${OPENAI_API_KEY:}
