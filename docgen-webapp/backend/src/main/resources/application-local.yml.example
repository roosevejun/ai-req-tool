agent:
  llm:
    openai:
      # AgentRouter (OpenAI Chat Completions compatible)
      baseUrl: "https://agentrouter.org/v1"
      model: "gpt-4o-mini"
      # Local-only config. Do NOT commit real keys.
      apiKey: ${AGENT_ROUTER_TOKEN:}
