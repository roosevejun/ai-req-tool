# 2026-03-30 开发日志

## 一、今日工作主线
今天的开发主要围绕两条主线推进：

1. 继续把页面类工作的执行流程固定为：`三省六部 -> ui-ux-pro-max-skill -> 实现`
2. 重点优化三个前端页面：
   - `项目管理中心入口页`
   - `传统创建项目页`
   - `AI 项目孵化页`

## 二、核心工作记录

### 1. 固化页面工作流程
- 继续校正页面类需求的处理顺序，明确不能再出现“先改页面、再补三省六部说明”的情况。
- 将该流程进一步沉淀到项目规范和技能约束中，作为页面工作的固定治理规则。

### 2. 项目管理中心入口页继续收口
相关文件：
- `frontend/src/views/ProjectManagementCenterView.vue`

今天对 `/projects` 做了多轮结构和排版优化，核心结果是：
- 删除了右侧“当前概览 / 项目准备度”块
- 页面回到更纯粹的“创建入口页”语义
- 将上方结构重整为：
  - 页面标题区
  - 两个紧凑的适用场景提示
  - 两个主入口卡
- 入口卡保留：
  - `传统创建项目`
  - `AI 项目孵化`

这轮之后，这页不再像“说明卡堆叠页”，更接近正式业务入口页。

### 3. 传统创建项目页继续做“正式办理页”收口
相关文件：
- `frontend/src/views/ProjectCreateFormView.vue`
- `frontend/src/components/project-create-form/ProjectCreateFormPanel.vue`

今天继续围绕 `/projects/create/form` 做收口：
- 去掉重复页头和重复办理说明
- 将页面继续整理为主办理区 + 辅助决策区
- 提升立项信息录入的优先级
- 保持“项目立项信息 -> 项目定位与背景 -> 客户与价值信息”的顺序更清晰

目标是让传统创建页更像正式立项办理页，而不是大表单堆叠页。

### 4. AI 项目孵化页整体重构
相关文件：
- `frontend/src/views/ProjectCreateAiView.vue`
- `frontend/src/components/project-create-ai/ProjectAiHeroPanel.vue`
- `frontend/src/components/project-create-ai/ProjectAiChatCard.vue`
- `frontend/src/components/project-create-ai/ProjectAiSetupCard.vue`
- `frontend/src/components/project-create-ai/ProjectAiDecisionPanel.vue`
- `frontend/src/components/project-create-ai/ProjectAiSidebar.vue`
- `frontend/src/components/project-create-ai/ProjectAiCreateCard.vue`
- `frontend/src/components/project-create-ai/ProjectAiResultCard.vue`

今天是这条线投入最多的一块，主要做了这些调整：

#### 4.1 顶部主面板整合
- 将“AI 项目孵化”和“框架成熟度”合并为一套顶部总面板
- 去掉重复说明和多余文案
- 顶部只保留：
  - 页面定位
  - 成熟度摘要
  - 当前状态
  - 切换动作

#### 4.2 主工作区重组
- 将主工作区明确拆成：
  - `AI 沟通`
  - `结果决策`
  两个标签页
- `AI 沟通` 中继续围绕：
  - 沟通输入
  - 沟通历史
- 优化了主工作区高度与下部留白，减少了无意义大空白

#### 4.3 辅助输入侧栏简化
- 将右侧从“第二个输入区”收成“资料补充工具侧栏”
- 删除与主沟通重复的起点输入
- 删除显式“启动 AI 会话”动作
- 收成：
  - 网站链接
  - 原文资料
  - 文件资料
  三种切换式输入方式

#### 4.4 结果决策区重构
- 将结果决策重构为更清晰的三段式：
  - 当前建议
  - 状态摘要
  - 核心信息与立项信息
- 合并重复建议，避免页面中出现两处同义建议
- 收掉多余标题、多余分隔和空态占位
- `项目信息` 区增加了更多已提炼项目事实展示
- 立项动作保留为：
  - `暂存项目信息`
  - `正式立项`

### 5. 修复结果决策组件结构问题
相关文件：
- `frontend/src/components/project-create-ai/ProjectAiDecisionPanel.vue`

今天还处理了一个影响前端构建的模板结构问题：
- 旧版 `ProjectAiDecisionPanel.vue` 存在模板标签未闭合
- 为避免继续在乱码环境里局部修补，直接整体重写为干净中文与有效结构
- 这样后续继续优化这块时，维护成本显著降低

## 三、验证记录

前端构建验证命令：

```bash
wsl.exe bash -lc '. "$HOME/.nvm/nvm.sh"; nvm use default >/dev/null; cd /mnt/g/Agent/docgen-webapp/frontend && npm run build'
```

最新构建结果：
- 前端构建通过

## 四、今天暴露出的关键问题

### 1. 页面类需求的执行顺序仍需要持续自检
虽然规则已经固定，但今天过程中仍出现过“先执行、后补三省六部”的偏差。

这说明后续要继续严格执行：
- 先三省六部
- 再 `ui-ux-pro-max-skill`
- 再实现

### 2. AI 项目孵化页仍然是后续重点演进区
虽然今天已经完成较大收口，但这页仍然是当前最复杂、最容易继续演进的一块：
- 主工作区和辅助侧栏的平衡
- 决策区的信息密度
- 用户对“暂存 / 正式立项”的判断路径

### 3. PowerShell 输出乱码仍会干扰局部修补
当前终端环境里，部分 Vue 文件在 `Get-Content` 输出时仍会出现乱码。
因此在个别复杂组件上，直接整体重写为干净中文和有效结构，比局部 patch 更稳。

## 五、下一步建议

1. 继续审 `AI 项目孵化` 页的整体气场，看是否还能进一步减少“卡片拼装感”
2. 继续检查 `结果决策` 是否还能再减层、减框、减重复提示
3. 继续按同样方法审其它中心页，确保“入口页 / 办理页 / 工作台”三类页面模型更加清楚
